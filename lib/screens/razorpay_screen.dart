
import 'package:flutter/material.dart';

class RazorpayScreen extends StatelessWidget {
  static const route = "RazorpayScreen";
  const RazorpayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}